const fr_onboarding = require("./fr/onboarding.json");
const en_onboarding = require("./en/onboarding.json");

const translations = {
  fr: { onboarding: fr_onboarding },
  en: { onboarding: en_onboarding },
};

export default translations;
